name: Push Release Notes to Beamer

on:
  release:
    types: [published]

jobs:
  push-release-notes:
    runs-on: ubuntu-latest
    steps:
    - name: Display workflow trigger message
      run: echo "Version released"
